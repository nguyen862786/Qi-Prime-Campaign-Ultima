// File scaffold cũ — KHÔNG được import trong app. Footer thực tế đang dùng nằm ở
// src/components/home/Footer.tsx. Giữ một component hợp lệ để không phá typecheck.
// Có thể xoá an toàn nếu không dùng tới.
export function Footer() {
  return null;
}

export default Footer;
